package ugr.gbv.cognimobile.interfaces;

/**
 * Interface that gives the activity the ability to display a dialog.
 */
public interface LoadDialog {
    /**
     * Displays a pop-up with the message.
     *
     * @param message the message to be displayed on the pop-up message.
     */
    void loadDialog(String message);
}
