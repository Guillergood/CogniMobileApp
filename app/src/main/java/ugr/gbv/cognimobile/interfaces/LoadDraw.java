package ugr.gbv.cognimobile.interfaces;

/**
 * Interface that gives the activity the ability to draw.
 */
public interface LoadDraw {
    /**
     * Tells the parent activity/fragment that it is ready to load the draw.
     */
    void loadDraw();
}
